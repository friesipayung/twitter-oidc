# twitter-oidc (not working right now)
A OpenID Connect wrapper run on Cloudflare Worker that implements the Twitter OAuth2 flow.

## Issue
Cloudflare Worker can't run with deps Passposrt.js and Twitter Strategy.